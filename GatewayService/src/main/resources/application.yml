spring:
  application:
    name: gateway-api
  cloud:
    gateway:
      globalcors:
        add-to-simple-url-handler-mapping: true
        corsConfigurations:
          '[/**]':
            allowedOriginPatterns:
              - 'http://localhost:5173'
              - 'http://127.0.0.1:5173'
              - 'http://localhost:*'
              - 'http://127.0.0.1:*'
              - 'https://nadja1908.github.io'
              - 'https://*.up.railway.app'
              - 'https://*.railway.app'
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: '*'
            allowCredentials: true
      routes:
        # URI: lokalno (IDE) → localhost; u Docker mreži compose postavlja RELATIONAL_/COLUMNAR_ SERVICE_URI.
        # Bez toga gateway na hostu ne rešava hostname relational-database-service i /api/auth/login vraća 500.
        - id: relational-student-curriculum
          uri: ${RELATIONAL_SERVICE_URI:http://localhost:9030}
          predicates:
            - Path=/api/student/me/curriculum-progress
        # Student: samo liste nepoloženih predmeta sa programskom stopom prolaznosti.
        - id: relational-student-unpassed-stats
          uri: ${RELATIONAL_SERVICE_URI:http://localhost:9030}
          predicates:
            - Path=/api/student/me/unpassed-subject-pass-rates
        - id: relational-assistant
          uri: ${RELATIONAL_SERVICE_URI:http://localhost:9030}
          predicates:
            - Path=/api/assistant/**
        # Šef / REST statistika — posebna ruta umesto duge liste predikata.
        - id: relational-program-statistics
          uri: ${RELATIONAL_SERVICE_URI:http://localhost:9030}
          predicates:
            - Path=/api/statistics/**
        - id: relational-auth-student-head-assistant
          uri: ${RELATIONAL_SERVICE_URI:http://localhost:9030}
          predicates:
            - Path=/api/auth/**,/api/student/me/profile,/api/student/me/subjects-grades,/api/student/me/gpa,/api/head/students,/api/head/programs,/api/head/program/**
        - id: columnar-statistics
          uri: ${COLUMNAR_SERVICE_URI:http://localhost:9050}
          predicates:
            - Path=/api/student/me/statistics,/api/head/subjects/analytics,/api/head/trends/pass-fail,/api/head/performance-overview,/api/stats/**

server:
  port: 9000

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health

logging:
  level:
    org.springframework.cloud.gateway: INFO
